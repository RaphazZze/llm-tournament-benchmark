# Judge Prompt

**Judge model:** Claude Opus 4.6 (Output Effort: Medium)

**Note:** Position is randomized (or balanced across runs) to mitigate position bias.

```
Both outputs below were generated from the same input using the same prompt.

Can you please evaluate the two below AI outputs?

Which model wins? Why? What does each do better or worse than the other? (table)

## Model A
[Model's generated response fenced in triple backquotes]

## Model B
[Model's generated response fenced in triple backquotes]
```
